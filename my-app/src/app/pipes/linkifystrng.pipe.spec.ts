import { LinkifystrngPipe } from './linkifystrng.pipe';

describe('LinkifystrngPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkifystrngPipe();
    expect(pipe).toBeTruthy();
  });
});
